[pytest]
markers =
    product: all product level tests
    integration: all integration level tests
    sanity: a subset of tests requiring to ensure the code changes are working as expected
addopts = -s -v --alluredir=tests/allure/results