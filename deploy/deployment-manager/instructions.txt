~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~


Instructions:

Please copy the command generated by kibana and paste it in the terminal to create a new deployment.
The command will look like this:

```
gcloud deployment-manager deployments create --automatic-rollback-on-error uri-test --template ./compute-engine.py
    --properties zone:<ZONE>,elasticAgentVersion:<VERSION>,fleetUrl:<URL>,enrollmentToken:<TOKEN>
```

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
