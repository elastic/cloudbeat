name: Publish-CloudFormation

on:
  push:
    branches:
      - main
      - "[0-9]+.[0-9]+"
    paths:
      - deploy/cloudformation/*.yml
      - .github/workflows/publish-artifacts.yml

jobs:
  publish_cloudformation:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.CSPM_CFT_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.CSPM_CFT_SECRET_ACCESS_KEY }}
          aws-region: us-east-2

      - name: Upload to S3 if elastic-agent CloudFormation template has changed
        run: |
          VERSION=$(grep defaultBeatVersion version/version.go | cut -f2 -d "\"")
          DATE=$(date +"%Y-%m-%d-%H-%M-%S")
          FILENAME="cloudformation-$VERSION-$DATE.yml"
          echo "Uploading $FILENAME to S3"
          aws s3 cp deploy/cloudformation/elastic-agent-ec2.yml s3://elastic-cspm-cft/$FILENAME

          FILENAME="cloudformation-organization-$VERSION-$DATE.yml"
          echo "Uploading $FILENAME to S3"
          aws s3 cp deploy/cloudformation/organization.yml s3://elastic-cspm-cft/$FILENAME
